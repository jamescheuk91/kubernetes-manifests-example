apiVersion: v1
kind: Service
metadata:
  namespace: dev
  name: oursky-sms
spec:
  type: NodePort
  selector:
    app: sms 
  ports:
    - name: rpc 
      nodePort: 31101
      port: 50051  
      targetPort: 50051
      protocol: TCP

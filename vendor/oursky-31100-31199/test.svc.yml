apiVersion: v1
kind: Service
metadata:
  namespace: dev
  name: oursky-test1
spec:
  type: NodePort
  selector:
    app: realtime-communication 
  ports:
    - name: rpc 
      nodePort: 31100
      port: 50051  
      targetPort: 50051
      protocol: TCP
